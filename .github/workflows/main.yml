name: Smoke Test

on:
  push:
    branches:
      - "*"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Run a basic test
        run: echo "GitHub Actions is working! 🎉"

      - name: Fail if needed
        run: |
          echo "Everything works, nothing to see here."
